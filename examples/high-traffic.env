# High-Traffic Configuration
# Optimized for high-traffic scenarios with longer intervals
# Reduces system load for busy servers

export CONTAINER="qbittorrent"
export LISTENING_PORT="6881"
export WGTUNNEL="10.2.0.1"  # Adjust based on your WireGuard config
export LOGFILE="/var/log/natpmp_forward.log"
export LOG_RETENTION_DAY="1"  # Shorter retention to save memory
export INTERVAL="120"         # Check every 2 minutes instead of 45 seconds